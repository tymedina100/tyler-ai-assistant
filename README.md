# Tyler AI Assistant

A beginner-friendly command-line AI assistant built with Python and the OpenAI API.

## Features

- Chat with an AI assistant from the terminal
- Short-term conversation memory during a session
- `/help` command
- `/clear` command to reset memory
- `/history` command to view current memory
- `/quit` command to exit
- API key stored safely in a `.env` file

## Setup

Create a virtual environment:

```powershell
python -m venv .venv